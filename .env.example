# Copy to .env.local and fill in real values as each integration is provisioned.
# See docs/deployment/environment-variables.md and src/env.ts for the full reference.

# Supabase (not yet provisioned)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# GA4 (not yet provisioned)
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Canonical site origin — used for metadata, sitemap, and robots absolute URLs.
# Optional in dev (falls back to the placeholder in src/lib/seo/site.ts); set
# per-environment in Vercel. TODO(seo.md): confirm apex vs. www before launch.
NEXT_PUBLIC_SITE_URL=

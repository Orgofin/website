# Local-dev defaults (checked in — no secrets here; real env lives in Vercel).
# Shows the experimental brand-palette switcher during the theme review.
# Set the same var in Vercel's PREVIEW environment (never Production) so
# co-founders can flip palettes on preview deployments.
NEXT_PUBLIC_BRAND_SWITCHER=1

/**
 * Barrel for the Vision page organisms (`docs/product/copy.md` §5). Ordered as
 * they appear on the page; the closing waitlist CTA is the shared `CTABand`
 * (see `@/components/sections`), not a Vision-specific organism.
 */
export { VisionHero } from "@/components/sections/vision/VisionHero";
export { VisionNarrative } from "@/components/sections/vision/VisionNarrative";
export { EnduringPromise } from "@/components/sections/vision/EnduringPromise";
